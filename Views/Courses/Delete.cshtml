@model CourseFormViewModel

@{
    ViewData["Title"] = "Удаление курса";
}

<partial name="_Alerts" />

<h1 class="h3 mb-3">Удаление курса</h1>
<p>Курс <strong>@Model.Name</strong> и все его задания будут удалены.</p>

<dl class="row">
    <dt class="col-sm-3">Преподаватель</dt>
    <dd class="col-sm-9">@Model.ProfessorName</dd>
    <dt class="col-sm-3">Описание</dt>
    <dd class="col-sm-9">@Model.Description</dd>
</dl>

<form asp-action="Delete" asp-route-id="@Model.Id" method="post">
    <button type="submit" class="btn btn-danger">Удалить</button>
    <a class="btn btn-outline-secondary" asp-action="Details" asp-route-id="@Model.Id">Отмена</a>
</form>
